# Finalization checkpoint

Hackathon finalization checkpoint.

- Dashboard polish is isolated in `dashboard-polish`.
- Candidate agent is preserved in `experiment-restored`.
- `main` is intentionally untouched by this checkpoint.
- Final merge should happen only after local validation.
